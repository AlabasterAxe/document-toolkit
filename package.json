{
  "name": "document-toolkit",
  "version": "1.0.0",
  "description": "Comprehensive toolkit for document processing, generation, and manipulation",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev",
    "doc:build": "tsx scripts/build-document.ts",
    "doc:preview": "tsx scripts/dev-server.ts"
  },
  "devDependencies": {
    "@types/react": "^19.1.13",
    "@types/ws": "^8.18.1",
    "commander": "^11.0.0",
    "gray-matter": "^4.0.3",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@10.15.0"
}
