# Use pnpm for package management
package-manager=pnpm

# Enable hoisting for shared dependencies
hoist-pattern[]=*

# Configure registry (optional - defaults to npm)
# registry=https://registry.npmjs.org/